javascript:(()=>{if(window.__pageAudioRecorderInstalled){alert('Page audio recorder is already installed. Press Insert to start/stop recording.');return;}window.__pageAudioRecorderInstalled=true;let stream=null,recorder=null,chunks=[],startedAt=0;const stopTracks=()=>{if(stream){for(const t of stream.getTracks())t.stop();stream=null;}};const download=(blob,name)=>{const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1e4);};const start=async()=>{try{stream=await navigator.mediaDevices.getDisplayMedia({video:true,audio:true});const audioTracks=stream.getAudioTracks();if(!audioTracks.length){alert('No audio track found. In the share dialog, choose a tab/window with audio and enable "Share tab audio".');stopTracks();return;}chunks=[];recorder=new MediaRecorder(new MediaStream(audioTracks),{mimeType:MediaRecorder.isTypeSupported('audio/webm;codecs=opus')?'audio/webm;codecs=opus':undefined});recorder.ondataavailable=e=>{if(e.data&&e.data.size)chunks.push(e.data);};recorder.onstop=()=>{const dur=Math.round((Date.now()-startedAt)/1e3);const ts=new Date().toISOString().replace(/[:.]/g,'-');download(new Blob(chunks,{type:chunks[0]?.type||'audio/webm'}),`page-audio-${ts}-${dur}s.webm`);chunks=[];stopTracks();recorder=null;console.log('[Recorder] Saved recording.');};recorder.start();startedAt=Date.now();console.log('[Recorder] Recording started. Press Insert to stop.');}catch(err){console.error('[Recorder] Could not start recording:',err);alert('Could not start recording. You must allow screen/tab capture and audio sharing.');stopTracks();}};const stop=()=>{if(recorder&&recorder.state!=='inactive'){recorder.stop();console.log('[Recorder] Stopping recording...');}else stopTracks();};window.addEventListener('keydown',e=>{if(e.key!=='Insert'||e.repeat)return; e.preventDefault();if(recorder&&recorder.state==='recording')stop();else start();},{capture:true});alert('Page audio recorder installed. Press Insert to start/stop recording.');})();
