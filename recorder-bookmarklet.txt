javascript:(()=>{if(window.__pageAVRecorderInstalled){alert('Page AV recorder is already installed. Press Insert to start/stop recording.');return;}window.__pageAVRecorderInstalled=true;let stream=null,recorder=null,chunks=[],startedAt=0;const stopTracks=()=>{if(stream){for(const t of stream.getTracks())t.stop();stream=null;}};const download=(blob,name)=>{const url=URL.createObjectURL(blob);const a=document.createElement('a');a.href=url;a.download=name;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1e4);};const start=async()=>{try{stream=await navigator.mediaDevices.getDisplayMedia({video:true,audio:true});const hasVideo=stream.getVideoTracks().length>0;if(!hasVideo){alert('No video track found. In the share dialog, choose a tab/window/screen to share.');stopTracks();return;}chunks=[];const mimeType=MediaRecorder.isTypeSupported('video/webm;codecs=vp9,opus')?'video/webm;codecs=vp9,opus':MediaRecorder.isTypeSupported('video/webm;codecs=vp8,opus')?'video/webm;codecs=vp8,opus':MediaRecorder.isTypeSupported('video/webm')?'video/webm':undefined;recorder=new MediaRecorder(stream,mimeType?{mimeType}:undefined);recorder.ondataavailable=e=>{if(e.data&&e.data.size)chunks.push(e.data);};recorder.onstop=()=>{const dur=Math.round((Date.now()-startedAt)/1e3);const ts=new Date().toISOString().replace(/[:.]/g,'-');download(new Blob(chunks,{type:chunks[0]?.type||mimeType||'video/webm'}),`page-av-${ts}-${dur}s.webm`);chunks=[];stopTracks();recorder=null;console.log('[Recorder] Saved audio+video recording.');};recorder.start();startedAt=Date.now();console.log('[Recorder] Recording started. Press Insert to stop.');}catch(err){console.error('[Recorder] Could not start recording:',err);alert('Could not start recording. You must allow screen/tab capture and audio sharing if you want system audio.');stopTracks();}};const stop=()=>{if(recorder&&recorder.state!=='inactive'){recorder.stop();console.log('[Recorder] Stopping recording...');}else stopTracks();};window.addEventListener('keydown',e=>{if(e.key!=='Insert'||e.repeat)return;e.preventDefault();if(recorder&&recorder.state==='recording')stop();else start();},{capture:true});alert('Page AV recorder installed. Press Insert to start/stop recording.');})();
